---
// Hero.astro - Premium Hero Section
---

<section class="hero">
  <div class="hero__background">
    <div class="hero__gradient"></div>
  </div>
  
  <div class="container hero__container">
    <div class="hero__content">
      <span class="hero__label animate-fade-in">Lujo Sostenible</span>
      <h1 class="hero__title animate-slide-up">
        Velas Artesanales<br />
        <span class="hero__title--accent">con Propósito</span>
      </h1>
      <p class="hero__description animate-slide-up">
        Creadas con ingredientes 100% naturales, ceras sostenibles y 
        fragancias botánicas. Una experiencia sensorial que cuida de ti y del planeta.
      </p>
      <div class="hero__actions animate-slide-up">
        <a href="#productos" class="btn btn-primary">
          Explorar Colección
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
        <a href="#historia" class="btn btn-secondary">
          Nuestra Historia
        </a>
      </div>
    </div>
    
    <div class="hero__visual">
      <div class="hero__image-frame">
        <img 
          src="/images/hero-candle.jpg" 
          alt="Vela artesanal Natuclean_py"
          class="hero__image"
        />
        <div class="hero__image-overlay"></div>
      </div>
      
      <!-- Floating elements for visual interest -->
      <div class="hero__floating hero__floating--1">
        <span class="hero__floating-text">100% Natural</span>
      </div>
      <div class="hero__floating hero__floating--2">
        <span class="hero__floating-text">Artesanal</span>
      </div>
    </div>
  </div>
  
  <!-- Scroll indicator -->
  <div class="hero__scroll">
    <span class="hero__scroll-text">Descubrir</span>
    <div class="hero__scroll-line"></div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
    background: var(--color-bg);
  }

  .hero__background {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero__gradient {
    position: absolute;
    inset: 0;
    background: 
      radial-gradient(ellipse 80% 50% at 50% -20%, rgba(25, 230, 60, 0.08) 0%, transparent 50%),
      radial-gradient(ellipse 60% 40% at 80% 60%, rgba(212, 163, 115, 0.05) 0%, transparent 40%),
      radial-gradient(ellipse 50% 50% at 20% 80%, rgba(25, 230, 60, 0.03) 0%, transparent 50%);
  }

  .hero__container {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-lg);
    align-items: center;
    padding-top: var(--space-xl);
    padding-bottom: var(--space-xl);
  }

  .hero__content {
    max-width: 600px;
  }

  .hero__label {
    display: inline-block;
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-primary);
    margin-bottom: var(--space-md);
    padding: 0.5rem 1rem;
    background: rgba(25, 230, 60, 0.1);
    border-radius: var(--radius-full);
  }

  .hero__title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 600;
    line-height: 1.1;
    margin-bottom: var(--space-md);
    color: var(--color-text);
  }

  .hero__title--accent {
    font-style: italic;
    color: var(--color-primary);
  }

  .hero__description {
    font-size: 1.1rem;
    line-height: 1.8;
    color: var(--color-text-light);
    margin-bottom: var(--space-lg);
    max-width: 480px;
  }

  .hero__actions {
    display: flex;
    gap: var(--space-sm);
    flex-wrap: wrap;
  }

  .hero__visual {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero__image-frame {
    position: relative;
    width: 100%;
    max-width: 450px;
    aspect-ratio: 3/4;
    /* Arched frame design */
    border-radius: 200px 200px 30px 30px;
    overflow: hidden;
    box-shadow: var(--shadow-xl);
    animation: float 6s ease-in-out infinite;
  }

  .hero__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .hero__image-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      transparent 0%,
      transparent 60%,
      rgba(0, 0, 0, 0.05) 100%
    );
    pointer-events: none;
  }

  .hero__image-frame:hover .hero__image {
    transform: scale(1.05);
  }

  .hero__floating {
    position: absolute;
    background: var(--color-white);
    padding: 0.75rem 1.25rem;
    border-radius: var(--radius-full);
    box-shadow: var(--shadow-md);
    animation: float 4s ease-in-out infinite;
  }

  .hero__floating--1 {
    top: 10%;
    right: -5%;
    animation-delay: 0s;
  }

  .hero__floating--2 {
    bottom: 20%;
    left: -5%;
    animation-delay: 2s;
  }

  .hero__floating-text {
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--color-text);
  }

  .hero__scroll {
    position: absolute;
    bottom: var(--space-lg);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-xs);
    z-index: 1;
  }

  .hero__scroll-text {
    font-family: var(--font-body);
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-text-light);
  }

  .hero__scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, var(--color-text-light) 0%, transparent 100%);
    animation: scrollPulse 2s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-15px);
    }
  }

  @keyframes scrollPulse {
    0%, 100% {
      opacity: 0.3;
      transform: scaleY(1);
    }
    50% {
      opacity: 1;
      transform: scaleY(1.2);
    }
  }

  /* Responsive */
  @media (max-width: 968px) {
    .hero__container {
      grid-template-columns: 1fr;
      text-align: center;
      gap: var(--space-md);
    }

    .hero__content {
      max-width: none;
      order: 1;
    }

    .hero__description {
      max-width: none;
    }

    .hero__actions {
      justify-content: center;
    }

    .hero__visual {
      order: 0;
    }

    .hero__image-frame {
      max-width: 300px;
    }

    .hero__floating {
      display: none;
    }
  }

  @media (max-width: 480px) {
    .hero__image-frame {
      max-width: 250px;
      border-radius: 150px 150px 20px 20px;
    }

    .hero__actions {
      flex-direction: column;
      width: 100%;
    }

    .btn {
      width: 100%;
    }
  }
</style>
