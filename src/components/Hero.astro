---
// Hero.astro - Elite Full-Screen Hero Section
// Inspired by HomeCraftedCandleCo premium aesthetic
---

<section class="hero-elite">
  <!-- Full-screen background image with overlay -->
  <div class="hero-elite__bg">
    <img 
      src="/images/hero-candle.jpg" 
      alt="Vela artesanal Natuclean"
      class="hero-elite__bg-image"
      loading="eager"
    />
    <div class="hero-elite__overlay"></div>
  </div>
  
  <!-- Content centered -->
  <div class="hero-elite__content">
    <span class="hero-elite__tagline">Artesanía Paraguaya</span>
    <h1 class="hero-elite__title">
      Velas que<br />
      <em>Iluminan el Alma</em>
    </h1>
    <p class="hero-elite__description">
      Creadas a mano con ingredientes naturales, cera de soja sostenible 
      y aceites esenciales puros. Una experiencia sensorial única.
    </p>
    <div class="hero-elite__actions">
      <a href="#productos" class="hero-elite__btn hero-elite__btn--primary">
        Ver Colección
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
      <a href="#historia" class="hero-elite__btn hero-elite__btn--secondary">
        Nuestra Historia
      </a>
    </div>
  </div>
  
  <!-- Scroll indicator -->
  <div class="hero-elite__scroll">
    <span class="hero-elite__scroll-text">Descubre</span>
    <div class="hero-elite__scroll-indicator">
      <div class="hero-elite__scroll-dot"></div>
    </div>
  </div>
  
  <!-- Trust badges floating -->
  <div class="hero-elite__badges">
    <div class="hero-elite__badge">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
      </svg>
      <span>100% Natural</span>
    </div>
    <div class="hero-elite__badge">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M12 2L2 7l10 5 10-5-10-5z"/>
        <path d="M2 17l10 5 10-5"/>
        <path d="M2 12l10 5 10-5"/>
      </svg>
      <span>Hecho a Mano</span>
    </div>
    <div class="hero-elite__badge">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="12" cy="12" r="10"/>
        <path d="M12 6v6l4 2"/>
      </svg>
      <span>+40h Duración</span>
    </div>
  </div>
</section>

<style>
  .hero-elite {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }
  
  /* Background image with parallax-like effect */
  .hero-elite__bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }
  
  .hero-elite__bg-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transform: scale(1.05);
    transition: transform 10s ease-out;
  }
  
  .hero-elite:hover .hero-elite__bg-image {
    transform: scale(1);
  }
  
  .hero-elite__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      rgba(26, 24, 22, 0.4) 0%,
      rgba(26, 24, 22, 0.2) 40%,
      rgba(26, 24, 22, 0.6) 100%
    );
  }
  
  /* Main content */
  .hero-elite__content {
    position: relative;
    z-index: 1;
    text-align: center;
    color: var(--color-white);
    max-width: 800px;
    padding: var(--space-md);
    animation: fadeInUp 1s ease-out;
  }
  
  .hero-elite__tagline {
    display: inline-block;
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: var(--space-md);
    padding: 0.5rem 1.5rem;
    border: 1px solid rgba(196, 167, 125, 0.4);
    border-radius: var(--radius-full);
    backdrop-filter: blur(4px);
    background: rgba(196, 167, 125, 0.1);
  }
  
  .hero-elite__title {
    font-family: var(--font-display);
    font-size: clamp(3rem, 8vw, 5.5rem);
    font-weight: 500;
    line-height: 1.05;
    letter-spacing: -0.02em;
    margin-bottom: var(--space-md);
    color: var(--color-white);
    text-shadow: 0 2px 40px rgba(0, 0, 0, 0.3);
  }
  
  .hero-elite__title em {
    font-style: italic;
    color: var(--color-accent);
  }
  
  .hero-elite__description {
    font-family: var(--font-body);
    font-size: clamp(1rem, 2vw, 1.2rem);
    font-weight: 300;
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.85);
    max-width: 550px;
    margin: 0 auto var(--space-lg);
    text-shadow: 0 1px 20px rgba(0, 0, 0, 0.3);
  }
  
  .hero-elite__actions {
    display: flex;
    gap: var(--space-sm);
    justify-content: center;
    flex-wrap: wrap;
  }
  
  /* Buttons */
  .hero-elite__btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    font-family: var(--font-body);
    font-size: 0.9rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-decoration: none;
    border-radius: var(--radius-sm);
    transition: all var(--transition-base);
    cursor: pointer;
  }
  
  .hero-elite__btn--primary {
    background: var(--color-accent);
    color: var(--color-black);
    border: none;
  }
  
  .hero-elite__btn--primary:hover {
    background: var(--color-white);
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
  }
  
  .hero-elite__btn--primary svg {
    transition: transform var(--transition-fast);
  }
  
  .hero-elite__btn--primary:hover svg {
    transform: translateX(4px);
  }
  
  .hero-elite__btn--secondary {
    background: transparent;
    color: var(--color-white);
    border: 1px solid rgba(255, 255, 255, 0.4);
    backdrop-filter: blur(4px);
  }
  
  .hero-elite__btn--secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: var(--color-white);
  }
  
  /* Scroll indicator */
  .hero-elite__scroll {
    position: absolute;
    bottom: var(--space-lg);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    z-index: 1;
    animation: fadeIn 1s ease-out 0.5s both;
  }
  
  .hero-elite__scroll-text {
    font-family: var(--font-body);
    font-size: 0.65rem;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.7);
  }
  
  .hero-elite__scroll-indicator {
    width: 24px;
    height: 40px;
    border: 2px solid rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    display: flex;
    justify-content: center;
    padding-top: 8px;
  }
  
  .hero-elite__scroll-dot {
    width: 4px;
    height: 8px;
    background: var(--color-accent);
    border-radius: 2px;
    animation: scrollBounce 2s ease-in-out infinite;
  }
  
  /* Trust badges */
  .hero-elite__badges {
    position: absolute;
    bottom: var(--space-lg);
    right: var(--space-lg);
    display: flex;
    gap: var(--space-sm);
    z-index: 1;
    animation: fadeIn 1s ease-out 0.8s both;
  }
  
  .hero-elite__badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: var(--radius-md);
    color: var(--color-white);
    transition: all var(--transition-base);
  }
  
  .hero-elite__badge:hover {
    background: rgba(255, 255, 255, 0.2);
  }
  
  .hero-elite__badge svg {
    stroke: var(--color-accent);
  }
  
  .hero-elite__badge span {
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.02em;
  }
  
  /* Animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  
  @keyframes scrollBounce {
    0%, 100% {
      transform: translateY(0);
      opacity: 1;
    }
    50% {
      transform: translateY(12px);
      opacity: 0.3;
    }
  }
  
  /* Responsive */
  @media (max-width: 1024px) {
    .hero-elite__badges {
      bottom: auto;
      top: var(--space-2xl);
      right: 50%;
      transform: translateX(50%);
      flex-wrap: wrap;
      justify-content: center;
      max-width: 90%;
    }
  }
  
  @media (max-width: 768px) {
    .hero-elite__title {
      font-size: clamp(2.5rem, 10vw, 4rem);
    }
    
    .hero-elite__badges {
      display: none;
    }
    
    .hero-elite__scroll {
      bottom: var(--space-md);
    }
    
    .hero-elite__btn {
      padding: 0.875rem 1.5rem;
      font-size: 0.85rem;
    }
  }
  
  @media (max-width: 480px) {
    .hero-elite__content {
      padding: var(--space-sm);
    }
    
    .hero-elite__actions {
      flex-direction: column;
      width: 100%;
      gap: 0.75rem;
    }
    
    .hero-elite__btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>
