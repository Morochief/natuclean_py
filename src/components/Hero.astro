---
---

<section class="hero premium-gradient">
  <div class="container hero-grid">
    <div class="hero-content animate-fade-in">
      <span class="badge">Artesanía Paraguaya</span>
      <h1 class="font-serif">Momentos de paz, <br/><span class="accent">rincones de luz pura</span></h1>
      <p>Velas artesanales creadas con almas botánicas y ceras 100% vegetales.</p>
      <div class="actions">
        <a href="#coleccion" class="btn btn-primary">Ver Colección</a>
        <a href="#historia" class="btn btn-outline">Nuestra Historia</a>
      </div>
    </div>
    <div class="hero-image animate-fade-in-delayed">
        <div class="image-wrapper">
            <img src="https://images.unsplash.com/photo-1602166551846-9d0b666aabd3?auto=format&fit=crop&q=80&w=1000" alt="Vela artesanal Natuclean" />
        </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 90vh;
    display: flex;
    align-items: center;
    padding: var(--spacing-xl) 0;
    overflow: hidden; /* Prevent spillover */
  }

  .container {
      width: 100%;
  }

  .hero-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--spacing-xl);
      align-items: center;
  }
  
  .badge {
    background: rgba(25, 230, 60, 0.1);
    color: var(--color-primary);
    padding: 0.5rem 1rem;
    border-radius: 2rem;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1rem;
    margin-bottom: var(--spacing-md);
    display: inline-block;
    border: 1px solid rgba(25, 230, 60, 0.2);
  }
  
  h1 {
    font-size: clamp(3rem, 5vw, 4.5rem);
    margin-bottom: var(--spacing-md);
    color: var(--color-text);
    line-height: 1.1;
  }
  
  .accent {
    color: var(--color-accent);
    font-style: italic;
    font-family: var(--font-serif);
  }
  
  p {
    font-size: 1.25rem;
    margin-bottom: var(--spacing-lg);
    opacity: 0.8;
    max-width: 90%;
    font-weight: 300;
  }
  
  .actions {
    display: flex;
    gap: var(--spacing-md);
  }
  
  .btn {
    padding: 1rem 2.5rem;
    border-radius: 2rem;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.3s ease;
    font-size: 1rem;
    letter-spacing: 0.5px;
  }
  
  .btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.1);
  }
  
  .btn-primary {
    background: var(--color-primary);
    color: white;
    border: 2px solid var(--color-primary);
  }

  .btn-primary:hover {
      background: #16cf35; /* Slightly darker green */
  }
  
  .btn-outline {
    border: 2px solid var(--color-text);
    color: var(--color-text);
    background: transparent;
  }

  .btn-outline:hover {
      background: var(--color-text);
      color: white;
  }

  .hero-image {
      position: relative;
  }

  .image-wrapper {
      border-radius: 200px 200px 0 0; /* Arch shape */
      overflow: hidden;
      height: 600px;
      width: 100%;
      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
      position: relative;
  }

  .image-wrapper img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 1.5s ease;
  }

  .hero-image:hover img {
      transform: scale(1.05);
  }

  @keyframes fade-in-up {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
  }

  .animate-fade-in {
      animation: fade-in-up 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }

  .animate-fade-in-delayed {
      opacity: 0;
      animation: fade-in-up 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards 0.3s;
  }

  @media (max-width: 968px) {
      .hero-grid {
          grid-template-columns: 1fr;
          text-align: center;
      }

      .hero-content {
          margin-bottom: var(--spacing-lg);
          display: flex;
          flex-direction: column;
          align-items: center;
      }

      p {
          max-width: 100%;
      }

      .image-wrapper {
          height: 400px;
          border-radius: var(--radius-lg);
      }
  }
</style>
