---
// RitualSection.astro - Premium Care Ritual Section with SVG Icons
---

<section class="ritual" id="ritual">
  <div class="container">
    <div class="ritual__header">
      <span class="ritual__label">Experiencia Completa</span>
      <h2 class="ritual__title">El Ritual de<br />Cuidado</h2>
      <p class="ritual__description">
        Disfruta al máximo de tu vela artesanal siguiendo estos simples pasos
      </p>
    </div>
    
    <div class="ritual__steps">
      <!-- Step 1 -->
      <div class="ritual__step">
        <div class="ritual__step-outer">
          <div class="ritual__step-inner">
            <svg class="ritual__step-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
            </svg>
          </div>
        </div>
        <span class="ritual__step-number">01</span>
        <h3 class="ritual__step-title">Prepara el Espacio</h3>
        <p class="ritual__step-description">
          Elige un lugar tranquilo. Retira la tapa y permite que la vela respire unos minutos antes de encender.
        </p>
      </div>
      
      <!-- Step 2 -->
      <div class="ritual__step">
        <div class="ritual__step-outer">
          <div class="ritual__step-inner">
            <svg class="ritual__step-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M17.657 18.657A8 8 0 016.343 7.343S7 9 9 10c0-2 .5-5 2.986-7C14 5 16.09 5.777 17.656 7.343A7.975 7.975 0 0120 13a7.975 7.975 0 01-2.343 5.657z"/>
              <path d="M9.879 16.121A3 3 0 1012.015 11L11 14H9c0 .768.293 1.536.879 2.121z"/>
            </svg>
          </div>
        </div>
        <span class="ritual__step-number">02</span>
        <h3 class="ritual__step-title">Enciende con Intención</h3>
        <p class="ritual__step-description">
          Enciende la mecha con atención plena. Visualiza tu intención mientras la llama cobra vida.
        </p>
      </div>
      
      <!-- Step 3 -->
      <div class="ritual__step">
        <div class="ritual__step-outer">
          <div class="ritual__step-inner">
            <svg class="ritual__step-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <circle cx="12" cy="12" r="10"/>
              <polyline points="12,6 12,12 16,14"/>
            </svg>
          </div>
        </div>
        <span class="ritual__step-number">03</span>
        <h3 class="ritual__step-title">Deja Arder</h3>
        <p class="ritual__step-description">
          Permite que arda hasta que la superficie se derrita completamente. Esto asegura una quemada uniforme.
        </p>
      </div>
      
      <!-- Step 4 -->
      <div class="ritual__step">
        <div class="ritual__step-outer">
          <div class="ritual__step-inner">
            <svg class="ritual__step-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
              <path d="M12 17c.5 0 1-.5 1-1s-.5-1-1-1-1 .5-1 1 .5 1 1 1z"/>
              <path d="M8 12c-1 2 0 4 4 5"/>
              <path d="M16 12c1 2 0 4-4 5"/>
            </svg>
          </div>
        </div>
        <span class="ritual__step-number">04</span>
        <h3 class="ritual__step-title">Extingue con Cariño</h3>
        <p class="ritual__step-description">
          Usa un soplido suave o la tapa para apagar. Nunca soples directamente. Deja enfriar antes de mover.
        </p>
      </div>
    </div>
    
    <div class="ritual__tips">
      <h4 class="ritual__tips-title">Cuidados Adicionales</h4>
      <div class="ritual__tips-grid">
        <div class="ritual__tip">
          <span class="ritual__tip-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="6" cy="6" r="3"/>
              <circle cx="6" cy="18" r="3"/>
              <line x1="20" y1="4" x2="8.12" y2="15.88"/>
              <line x1="14.47" y1="14.48" x2="20" y2="20"/>
              <line x1="8.12" y1="8.12" x2="12" y2="12"/>
            </svg>
          </span>
          <span>Recorta la mecha a 5mm</span>
        </div>
        <div class="ritual__tip">
          <span class="ritual__tip-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <polyline points="12,6 12,12 16,14"/>
            </svg>
          </span>
          <span>Máximo 4 horas continuas</span>
        </div>
        <div class="ritual__tip">
          <span class="ritual__tip-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2c-4 4-8 6-8 10a8 8 0 0016 0c0-4-4-6-8-10z"/>
            </svg>
          </span>
          <span>Almacena en lugar fresco</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .ritual {
    padding: var(--space-xl) 0;
    background: var(--color-bg);
  }
  
  .ritual__header {
    text-align: center;
    max-width: 600px;
    margin: 0 auto var(--space-xl);
  }
  
  .ritual__label {
    display: inline-block;
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-primary);
    margin-bottom: var(--space-sm);
    padding: 0.5rem 1rem;
    background: rgba(25, 230, 60, 0.1);
    border-radius: var(--radius-full);
  }
  
  .ritual__title {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: var(--space-sm);
    line-height: 1.2;
  }
  
  .ritual__description {
    font-size: 1.1rem;
    color: var(--color-text-light);
  }
  
  .ritual__steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
    position: relative;
  }
  
  /* Connector line */
  .ritual__steps::before {
    content: '';
    position: absolute;
    top: 40px;
    left: 15%;
    right: 15%;
    height: 2px;
    background: linear-gradient(90deg, var(--color-primary) 0%, transparent 100%);
    z-index: 0;
  }
  
  .ritual__step {
    position: relative;
    text-align: center;
    padding: var(--space-lg) var(--space-sm) var(--space-md);
    z-index: 1;
  }
  
  .ritual__step-outer {
    width: 80px;
    height: 80px;
    margin: 0 auto var(--space-md);
    background: var(--color-white);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-md);
    transition: all var(--transition-base);
  }
  
  .ritual__step-inner {
    width: 70px;
    height: 70px;
    background: rgba(25, 230, 60, 0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--transition-base);
  }
  
  .ritual__step:hover .ritual__step-outer {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
  }
  
  .ritual__step:hover .ritual__step-inner {
    background: var(--color-primary);
  }
  
  .ritual__step-icon {
    width: 32px;
    height: 32px;
    color: var(--color-primary);
    transition: all var(--transition-base);
  }
  
  .ritual__step:hover .ritual__step-icon {
    transform: scale(1.1);
    color: var(--color-white);
  }
  
  .ritual__step-number {
    display: block;
    font-family: var(--font-display);
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: var(--space-sm);
    letter-spacing: 0.1em;
  }
  
  .ritual__step-title {
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: var(--space-xs);
    color: var(--color-text);
  }
  
  .ritual__step-description {
    font-size: 0.85rem;
    color: var(--color-text-light);
    line-height: 1.6;
  }
  
  .ritual__tips {
    background: var(--color-bg-alt);
    border-radius: var(--radius-lg);
    padding: var(--space-lg);
    max-width: 800px;
    margin: 0 auto;
  }
  
  .ritual__tips-title {
    font-family: var(--font-display);
    font-size: 1.25rem;
    text-align: center;
    margin-bottom: var(--space-md);
    color: var(--color-text);
  }
  
  .ritual__tips-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
  }
  
  .ritual__tip {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-sm);
    padding: var(--space-sm) var(--space-md);
    background: var(--color-white);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
    transition: all var(--transition-base);
  }
  
  .ritual__tip:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
  }
  
  .ritual__tip-icon {
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-primary);
    border-radius: 50%;
    flex-shrink: 0;
  }
  
  .ritual__tip-icon svg {
    width: 14px;
    height: 14px;
    color: var(--color-white);
  }
  
  .ritual__tip span:last-child {
    font-size: 0.85rem;
    color: var(--color-text);
    font-weight: 500;
  }
  
  @media (max-width: 968px) {
    .ritual__steps {
      grid-template-columns: repeat(2, 1fr);
    }
    
    .ritual__steps::before {
      display: none;
    }
    
    .ritual__tips-grid {
      grid-template-columns: 1fr;
    }
  }
  
  @media (max-width: 480px) {
    .ritual__steps {
      grid-template-columns: 1fr;
    }
    
    .ritual__step-outer {
      width: 64px;
      height: 64px;
    }
    
    .ritual__step-inner {
      width: 54px;
      height: 54px;
    }
    
    .ritual__step-icon {
      width: 24px;
      height: 24px;
    }
  }
</style>
